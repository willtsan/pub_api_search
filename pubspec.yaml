name: pub_api_search
description: A new Flutter project.
publish_to: 'none' # Remove this line if you wish to publish to pub.dev


version: 1.0.0+1

environment:
  sdk: '>=2.18.5 <3.0.0'

dependencies:
  flutter:
    sdk: flutter

  cupertino_icons: ^1.0.2
  mix:
    git: 
      url: https://github.com/conceptadev/mix.git
      ref: feature/gesture-box # Branch usada para fix do onPress do Presable ser utilizado
  get: ^4.6.5
  dartz: ^0.10.1
  pub_api_client: ^2.2.1
  shared_preferences: ^2.0.18

dev_dependencies:
  flutter_test:
    sdk: flutter

  flutter_lints: ^2.0.0
flutter:
  uses-material-design: true